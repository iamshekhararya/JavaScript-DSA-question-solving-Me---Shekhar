// First DSA question -->  Find largest value in Array --> JavaScript

let arr = [1,2,3,5,8,6,7,9,6];
let max = arr[0];
for(let i = 0; i < arr.length; i++){
 if(arr[i] > max){
  max = arr[i];
  }
}
 console.log("Maximun Value of Array: " , max);

// Output ---> 9
